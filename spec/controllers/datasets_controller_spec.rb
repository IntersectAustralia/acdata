require 'spec_helper'

describe DatasetsController do
  it "should allow selecting a sample for a dataset to be created under" do
    pending
    #save_sample_select
  end

  it "should create and add attachments" do
    pending
    #create_and_add_attachments
  end

  it "should set prompts and filters for the upload page" do
    pending
    #upload
  end

  it "should set a flag if the metadata page was reached from the show page" do
    pending
    #metadata (@from_show_page)
  end

  it "should create zip file for download" do
    pending
    #download
  end

end
